// Read in the property values of testObj using dot notation. Set the variable hatValue equal to the object's property hat and set the variable shirtValue equal to the object's property shirt.

// Passed:hatValue should be a string
// Passed:The value of hatValue should be the string ballcap
// Passed:shirtValue should be a string
// Passed:The value of shirtValue should be the string jersey
// Passed:You should use dot notation twice

// Setup
const testObj = {
    "hat": "ballcap",
    "shirt": "jersey",
    "shoes": "cleats"
  };
  
  // Only change code below this line
  hat : "ballcap"
  shirt: "jersey"  // Change this line
  
  const hatValue = testObj.hat
  const shirtValue = testObj.shirt