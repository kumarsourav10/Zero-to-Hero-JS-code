// Read the values of the properties an entree and the drink of testObj using bracket notation and assign them to entreeValue and drinkValue respectively.

// Waiting:entreeValue should be a string
// Waiting:The value of entreeValue should be the string hamburger
// Waiting:drinkValue should be a string
// Waiting:The value of drinkValue should be the string water
// Waiting:You should use bracket notation twice

// Setup
const testObj = {
    "an entree": "hamburger",
    "my side": "veggies",
    "the drink": "water"
  };
  
  // Only change code below this line
  const entreeValue = testObj["an entree"];   // Change this line
  const drinkValue = testObj["the drink"];    // Change this line